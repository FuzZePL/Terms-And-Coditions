import 'package:flutter/material.dart';

class RegisterWidget3 extends StatelessWidget {
  const RegisterWidget3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
